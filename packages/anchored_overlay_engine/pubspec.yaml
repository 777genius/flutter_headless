name: anchored_overlay_engine
description: Anchored overlay lifecycle engine (positioning, policies, insertion).
version: 0.0.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.1
