import 'package:jaspr/jaspr.dart';

class DocsDartPadRuntime extends StatelessComponent {
  const DocsDartPadRuntime({super.key});

  @override
  Component build(BuildContext context) => Component.fragment(const []);
}
