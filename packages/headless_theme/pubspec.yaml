name: headless_theme
description: Capability-based theme runtime and overrides for Headless.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0
  headless_tokens:
    path: ../headless_tokens
  headless_foundation:
    path: ../headless_foundation
  headless_contracts:
    path: ../headless_contracts

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.1

