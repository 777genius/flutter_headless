# Changelog

## 1.1.0

- Add `enableIMEPersonalizedLearning` and `autofillHints` to `RTextField`.
- Forward the new input configuration through the editable text factory and
  render view.

## 1.0.0

- First stable public release of `headless_textfield`.
