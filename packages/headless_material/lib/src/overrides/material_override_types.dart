/// Shared types for Material preset advanced overrides.
library;

/// Density knobs for Material components.
enum MaterialComponentDensity {
  compact,
  standard,
  comfortable,
}

/// Corner radius policy for Material components.
enum MaterialCornerStyle {
  sharp,
  rounded,
  pill,
}
