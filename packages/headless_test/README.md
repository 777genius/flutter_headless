# headless_test

Test helpers for Headless packages and apps.

This package contains reusable utilities for validating behavior that is easy
to regress in headless widgets:

- accessibility semantics
- overlay contracts
- keyboard interaction
- focus handling

Use it in package tests when you want the same conformance helpers that the
Headless presets use internally.
