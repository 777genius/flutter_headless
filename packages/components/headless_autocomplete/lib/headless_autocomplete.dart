// Headless Autocomplete component.
//
// Provides [RAutocomplete] - a composable input + menu widget that owns
// behavior (options, selection sync, keyboard, overlay) but delegates
// visuals to renderer capabilities.
library;

export 'r_autocomplete.dart';
