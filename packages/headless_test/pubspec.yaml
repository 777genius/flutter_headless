name: headless_test
description: Optional test helpers for Headless (a11y/overlay/focus/keyboard).
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  anchored_overlay_engine:
    path: ../anchored_overlay_engine
  headless_tokens:
    path: ../headless_tokens
  headless_foundation:
    path: ../headless_foundation
  headless_contracts:
    path: ../headless_contracts
  headless_theme:
    path: ../headless_theme

dev_dependencies:
  lints: ^5.1.1

