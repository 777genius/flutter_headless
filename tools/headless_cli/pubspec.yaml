name: headless_cli
description: Optional tooling for Headless (W3C tokens import + skeleton generation).
version: 0.0.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  args: ^2.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

